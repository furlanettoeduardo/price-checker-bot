# Requisições HTTP
requests==2.31.0

# Bypass Cloudflare JS challenge (usado automaticamente quando requests recebe 403)
cloudscraper>=1.2.71

# Parsing de HTML
beautifulsoup4==4.12.3
lxml>=5.2.1

# Integração com Google Sheets
gspread==6.1.2
oauth2client==4.1.3

# Opcional: análise de dados / relatórios
pandas>=2.2.2

# Opcional: detecção de domínio mais precisa (recomendado)
tldextract>=3.4.0

# Opcional: scraping de páginas com JavaScript / Cloudflare Under Attack Mode
# playwright install chromium
# pip install playwright
